services:
  api:
    build: .
    ports:
      - "8100:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/riskengine
      - REDIS_URL=redis://redis:6379
    depends_on:
      - db
      - redis
    restart: on-failure

  db:
    image: postgres:15
    environment:
      - POSTGRES_DB=riskengine
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: on-failure

  redis:
    image: redis:7
    ports:
      - "6380:6379"
    restart: on-failure

volumes:
  pgdata: